# E2→F1 严格重建与 M1 候选（2026-07-30）

## 结论

本机缺失原 F1 checkpoint 后，使用保留的官方数据、同源
`preliminary/seed42` split 和原始配置，重新训练 E2 CE5+MixUp 父模型，再在完全
相同的 train/val 上训练 F1 visual LoRA。父子 lineage 审计无交叉泄漏、无标签错配，
F1 promotion 通过。

重建 F1 的 M1 `crop160/top5/local_weight0.35` 相对自身 global：

- raw micro：`70.5409% → 71.8011%`，`+1.2602pp`；
- trusted macro：`80.4841% → 81.3955%`；
- proxy macro：`79.4459% → 80.1684%`；
- clean-core micro：`81.2850% → 82.2398%`，`+0.9548pp`；
- clean-core macro：`82.1028% → 82.9285%`；
- 类别覆盖：500。

该单 checkpoint 提交包已审计通过，平台最终为 **62.9791%**，状态
`platform_valid_not_promoted`。它比 A2 STRICT + M1 62.6870% 高
`+0.2921pp`，但仍比已报告的原 F1 + M1 63.3276% 低 `0.3485pp`。
这说明同 split F1 重建改善了 A2 checkpoint 表征，但没有逐字节复原原 F1 的平台能力。

## E2 父模型重建

- 配方：冻结 OpenAI CLIP ViT-B/32，只训练线性头；CE warmup 5 epoch，随后 GCE
  `q=0.5`；pixel MixUp alpha 0.2 / probability 0.2；最多 50 epoch，patience 10。
- 数据：train 92,902、val 10,316；官方数据，无外部数据。
- 最佳：epoch 39，训练内 raw `70.3470%`、500 类、视觉漂移 0。
- archived E2 父模型 raw 为 `70.2307%`；重建高 `+0.1163pp`。
- F1 epoch-0 在线复评：raw `70.3180%`、clean-core `80.8757%`、500 类、漂移
  `0.0001%`。
- checkpoint SHA-256：
  `1e4cd9523ec73c2f6ee42f7da18d1c686e9680ee832739b58e791b89c292f8a9`。

Lineage 审计：

- parent/child train SHA-256：
  `a726b8a3ca8bc5857136106aca80f01d557104d3661ef92ccedfb2c0ea087875`；
- parent/child val SHA-256：
  `54a790b35f836cfba4c19cbb5fe38c4b1b37aab62cc9d477f9285496b2d5568e`；
- child val in parent train：0；
- child train in parent val：0；
- label mismatches：0。

## F1 visual LoRA 重建

- 最后 4 个 ViT block 的 Q/V/out，rank 8、alpha 8；
- 403,956 / 88,253,172 参数可训练；
- trust `>=0.70` 才接受分类监督；GCE `q=0.5`；feature anchor weight 2.0；
- 最佳 epoch 4：raw `70.5409%`、trusted macro `80.4841%`、proxy macro
  `79.4459%`、clean-core `81.2850%`、flip agreement `88.9298%`、漂移
  `0.3982%`、500 类；
- 相对 epoch 0：raw `+0.2230pp`、clean-core `+0.4092pp`；
- promotion：全部检查通过；
- checkpoint SHA-256：
  `805e0df7d2ee7dfb2481a08664b71eb5f7ed600f27f1d5af726c926f3e6fb281`；
- promotion JSON SHA-256：
  `19b26ac24ecdb7e7a697c6acd8e4bb3b91494a6366e4d603cb9e98f067ff27db`。

与 archived F1 epoch 4 对比：重建 raw 低 `0.1357pp`，clean-core 低
`0.2456pp`，flip agreement 高 `0.0872pp`，漂移更小。由于 checkpoint 并非逐字节
复原，已报告 63.3276% 只能作为平台锚点，不能当作本候选分数。

## M1 权重复核

固定 `crop160/top5`，只复核 `local_weight ∈ {0.25,0.30,0.35,0.40,0.45,0.50}`。
所有点相对 global 的 raw、trusted、proxy、clean-core 都为正，且保持 500 类。

`0.40` 的 raw 与 clean-core micro 分别比 `0.35` 高约 `0.0485pp` 和
`0.0546pp`，但 trusted macro、proxy macro、clean-core macro 分别低
`0.0178pp`、`0.0867pp`、`0.0925pp`。因此按多指标裁决选择 `0.35`，不追单一
raw 峰值。

权重 sweep JSON SHA-256：
`95e11064ce85b704f4f68d731f1e4916970589a49408506c386fbcbd74e44766`。

## 平台包

- 路径：
  `reproducibility/aegis_f1/outputs/F1_VISUAL_LORA_CLEAN_CORE_REBUILD_R1/seed42/submissions/m1_w035/submission.zip`
- prediction CSV SHA-256：
  `a992d8c11c203ac89a1335c0ff8f833fe56c686115303c67347cc6c96b9dbb4b`
- submission ZIP SHA-256：
  `02d37906accdf6b49e40733b4e675220f0177b5d71c5662984de68df5e781bb6`
- 预测数：24,967；
- 类别数：500；
- 唯一图片名：24,967；
- 损坏图：0；
- `aegis-audit-submission --allow-tta`：passed。

- 平台：**62.9791%**；
- 相对 A2 STRICT + M1 62.6870%：`+0.2921pp`；
- 相对已报告原 F1 + M1 63.3276%：`−0.3485pp`；
- 距离 70 分：`7.0209pp`；
- 已按 checkpoint、prediction CSV 和 ZIP SHA-256 精确登记到
  `results/current_platform_summary.csv` 与 `results/submission_registry.csv`。

## 回归验证

- localization/local-adapter/checkpoint/model/submission/TTA 相关测试：29 passed；
- Aegis 子工程全量：75 passed、1 failed；
- 唯一失败为既有 Phase 4 配置使用 `stage: p4_ablation`，而 validator 只接受
  preliminary/repechage/semifinal；两个本轮新增重建配置均独立通过加载校验；
- `git diff --check`：passed。
