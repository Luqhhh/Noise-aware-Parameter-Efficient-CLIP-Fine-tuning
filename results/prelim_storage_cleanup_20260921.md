# 初赛大文件清理（2026-09-21）

用户在复赛数据导入后授权删除不再需要的初赛大文件。根据 [复赛准备文档](../docs/repechage_prep_20260831.md) 的阶段隔离约定，初赛拟合权重、特征缓存、伪标签与类别原型不用于复赛初始化或训练。

## 已完成清理

- 删除 86 个普通文件，文件逻辑大小合计 **15,887,954,854 bytes（14.797 GiB）**，另移除 2 个对应的本地符号链接；此统计不是底层磁盘实际回收块数。
- 删除初赛 v2–v10 的剩余候选权重、诊断张量、logits、几何框与监督二进制缓存。
- 删除 `reproducibility/aegis_f1/outputs/` 中初赛全量微调、Adapter 和自训练权重，以及 `artifacts/trust/` 中的初赛教师张量。
- 清理根目录 `cache/` 内的旧缓存文件。
- 精确路径、逐文件字节数和符号链接标记见 [机器可读删除清单](prelim_storage_cleanup_20260921.json)。所有登记路径已验证不存在。

## 保留与可恢复性

保留复赛 `train.zip`、`test.zip`、`train/`、`test/`，以及源码、配置、实验报告、历史成绩、较小的 CSV/ZIP 提交证据和 Git 已跟踪的历史文件。复赛数据导入指纹与规模见 [复赛数据集元信息](../docs/rematch_dataset_20260921.md)。

本次删除项均未被 Git 跟踪，不能通过 Git 恢复。初赛旧数据也已在复赛导入时删除，因此本地不再具备直接重放初赛训练或模型推理的全部资产。历史报告中的权重路径、保留声明和可复现状态描述属于当时快照；本清理记录更新其当前本地可用性，不修改既有成绩或哈希记录。

本次为存储清理，未运行训练或生成新提交。
