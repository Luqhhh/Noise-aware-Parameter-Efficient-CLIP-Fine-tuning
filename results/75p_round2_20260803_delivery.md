# 交付摘要（2026-08-03 定稿）

## 最终提交包（平台实测）
**`flat_child_l040_f050`**（桌面 submission.zip）：**平台 67.7014%**，新审计
完整平台最佳（+0.0161pp vs A12_CORR）。ZIP `db2f42f9…74ba`。
A12_CORR 包（`current_best_a12corr`）为前一最佳（67.6853%）。

## 本轮优化点（75p_round2）
| 候选 | 结果 |
|---|---|
| clip_letterbox 推理 | ❌ 全部指标下降（raw −5pp） |
| trust-aware 父模型（丢弃版） | ❌ clean 0.7835 < 0.8088 |
| trust-aware 父模型（连续加权版） | ⚠️ clean 0.8085 ≈ 噪声盲 0.8088，持平 |
| **flat-LR 子模型（schedule 16）** | ✅ bare clean 0.8193（+0.1pp）；平台 67.7014%（+0.016pp）转正 |

## 结论
- 信任机制在父阶段无明确收益（父模型主要价值在全部数据 + GCE + MixUp）。
- flat-LR 子模型微弱转正：裸表征 +0.1pp 虽未转化为 M1+flip 优势，平台仍
  判定更优——平台是唯一裁判。
- **75% 目标现实评估**：本地 clean-core 0.82 → 平台 67.7%（14pp 分布 gap）。
  需 clean-core ~0.89 才能到 75%，当前增量杠杆（±0.5pp clean）不足以达成。
  本轮实际成果 67.69 → 67.70%（+0.016pp）。
