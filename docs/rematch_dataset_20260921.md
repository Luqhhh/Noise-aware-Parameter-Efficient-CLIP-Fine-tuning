# 复赛数据集元信息（2026-09-21）

## 来源与本地位置

用户提供的复赛数据于 2026-09-21（Asia/Shanghai）复制、校验并解压完成，项目根目录为 `/home/lux1/noise`。

| 数据 | Windows 来源 | 项目内压缩包 | 项目内解压目录 |
|---|---|---|---|
| 训练集 | `D:\data\已加速- 复赛数据集\train.zip` | `train.zip` | `train/` |
| 测试集 | `D:\data\已加速- 复赛数据集\test.zip` | `test.zip` | `test/` |

WSL 来源目录为 `/mnt/d/data/已加速- 复赛数据集/`。D 盘原始压缩包保留。项目内压缩包与解压目录均由 `.gitignore` 排除，不随 Git 提交。

## 规模与组织

以下统计来自压缩包目录清单及解压后文件核对；字节数均为精确值，解压大小不包含文件系统元数据开销。

| 指标 | 训练集 | 测试集 |
|---|---:|---:|
| 图片数 | 148,695 | 37,444 |
| 类别目录数 | 750 | 无类别子目录 |
| ZIP 条目数（含目录） | 149,446 | 37,445 |
| 压缩包大小（字节） | 30,257,831,264 | 1,534,861,286 |
| 解压文件总大小（字节） | 30,957,960,910 | 1,534,554,926 |
| 文件扩展名 | `.jpg` | `.jpg` |

- 训练集结构：`train/<类别目录>/<图片名>.jpg`；类别目录为 `0000` 至 `0749`，每类图片数最少 5 张、最多 248 张。
- 测试集结构：`test/<图片名>.jpg`，图片直接位于测试目录中。
- 类别映射应由目录名按字典序排序生成，并保存、复用映射文件，不在代码中硬编码。

## 完整性校验

复制时对源文件内容计算 SHA-256，复制后重新读取目标压缩包计算 SHA-256，两者一致：

| 压缩包 | SHA-256 |
|---|---|
| `train.zip` | `11b70e8b86e7b093cfeb117f5a8422b38c0f7011e41344cc14ae80bc29f3d319` |
| `test.zip` | `fc9b7eb5ec76724bdc00e06e31cedd333fbd21685f9111dd342934796e678d6e` |

解压时逐文件读取并通过 ZIP CRC 校验；解压后的文件数和总字节数与 ZIP 清单一致。替换完成后再次核对图片数量与训练类别目录数。以上校验验证传输和压缩包完整性，导入当时尚未进行逐图片解码检查或标签质量审计。随后 2026-09-21 已完成全部图片严格解码（失败 0）和内容组隔离划分，实际结果见[复赛执行记录](rematch750_execution_20260921.md)；未进行标签清洗。

可在项目根目录重新核对压缩包指纹：

```bash
sha256sum train.zip test.zip
```

## 替换范围与训练准备

两个新数据集均完成校验后，替换了项目根目录下的旧 `train/`、`test/`，并删除旧 `train_dedup/` 去重数据副本。临时导入目录和替换时的旧数据备份目录均已清理。随后按用户要求删除初赛模型权重和二进制缓存；历史报告及较小的提交证据保留，详见[初赛大文件清理记录](../results/prelim_storage_cleanup_20260921.md)。初赛拟合权重及数据派生产物不得跨阶段复用，参见[复赛准备文档](repechage_prep_20260831.md)。

本记录最初对应数据导入。随后已完成独立分组划分、官方特征重建及 RM-LP 训练/提交校验，见[当前复赛执行记录](rematch750_execution_20260921.md)。以下为阶段迁移要求，不能直接沿用初赛派生资产：

1. 将所用配置的类别数从初赛的 500 更新为 750，并确认训练、测试路径指向本次导入目录。
2. 为复赛使用独立输出目录，重新生成训练/验证划分、`class_to_idx.json` 和 `idx_to_class.json`。
3. 重新生成与数据绑定的特征缓存、样本权重及去重结果；旧数据划分和派生文件不能直接视为本次复赛数据的对应产物。
4. 复赛提交格式和允许的训练、推理方法应依据复赛规则另行核对；历史初赛成绩与提交产物不是复赛测量结果。
